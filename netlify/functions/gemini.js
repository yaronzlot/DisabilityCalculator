const BOOK_CONTENT = `
ספר המבחנים לנכות כללית ונפגעי עבודה - ביטוח לאומי
תקנות הביטוח הלאומי (קביעת דרגת נכות לנפגעי עבודה) תשט"ז 1956

====== פרק ראשון: מחלות פנימיות ======

סעיף 4 - סוכרת:
(א) היפרגליקמיה או סוכרת, בלא טיפול תרופתי: 0%
(ב) סוכרת בטיפול פומי או אנלוג GLP-1: 10%
(ג) סוכרת בטיפול אינסולין או בטיפול משולב עם אינסולין: 20%
(ד) סוכרת לא מאוזנת המטופלת טיפול אינטנסיבי באינסולין (4 זריקות ומעלה ביום או משאבת אינסולין): 40%
(ה) סוכרת עם אי יציבות קלינית בולטת: 50%
(ו) סוכרת עם סיבוך באחד מאברי המטרה בחומרה 30% לפחות: 65%
(ז) סוכרת עם שלושה סיבוכים לפחות באברי המטרה: 100%

סעיף 5 - מחלות ריאה כרוניות (COPD):
(א) עם FEV1 או DLCO בין 65% ל-74%: 20%
(ב) עם FEV1 או DLCO בין 50% ל-64%: 40%
(ג) עם FEV1 בין 30% ל-49% או DLCO בין 35% ל-49%: 60%
(ד) עם FEV1 קטן מ-30% או DLCO קטן מ-35%: 100%

סעיף 6 - אסתמה:
(א) ניקוד 0: 0%; (ב) ניקוד 1-3: 10%; (ג) ניקוד 4-6: 20%
(ד) ניקוד 7-9: 40%; (ה) ניקוד 10 ומעלה: 60%
(ו) ניקוד מעל 5 עם שני אשפוזים בטיפול נמרץ: 80%
(ז) הנשמה מלאכותית קבועה: 100%

סעיף 9 - מחלות הלב:
מקטע פליטה >60%, NYHA I: ללא נכות
מקטע פליטה 45-60%, NYHA II: 25%
מקטע פליטה 30-44%, NYHA III: 50%
מקטע פליטה <30%, NYHA IV: 100%
יתר לחץ דם: לחץ 95-110 ממ"כ ללא הגדלת לב: 10%; עם הגדלת לב: 30%

====== מחלות הדם ======

סעיף 2 - אנמיה:
(א) אנמיה שחלפה: 0%; (ב) אנמיה כרונית קלה: 10%
(ג) תלות בעירויי דם: 30%; (ד) תלות קבועה: 50%; (ה) יותר ממנה לחודש: 100%

====== פרק שני: נוירולוגיה ======

סעיף 29 - תסמונות מוחיות:
חולשת פלג גוף (hemiparesis):
קלה מאוד: דומיננטי 10%, לא דומיננטי 5%
קלה - הליכה עצמאית: דומיננטי 30%, לא דומיננטי 20%
בינונית - הליכה בעזרים: דומיננטי 50%, לא דומיננטי 40%
קשה - ללא הליכה עצמאית: דומיננטי 80%, לא דומיננטי 60%
שיתוק מלא (hemiplegia): 100%

סעיף 30 - אפילפסיה:
ללא התקפים שנתיים: 0%; עד 2 התקפים בשנה: 20%
3 התקפים: 40%; 4 התקפים: 60%; 5-6 התקפים: 80%; מעל 6: 100%

סעיף 31-32 - עצבים היקפיים:
פגימה בעצב Radialis: קלה ימין 40%, שמאל 30%; מלא ימין 40%, שמאל 30%
פגימה בעצב Sciatic: קלה 10%; בינונית 20%; מלא 40%

====== פרק שלישי: מחלות נפש ======

סעיף 34 - הפרעות במצב הרוח, חרדה, PTSD:
(א) רמיסיה מלאה: 0%; (ב) הגבלה קלה: 10%; (ג) הפרעה בינונית: 20%
(ד) הגבלה ניכרת: 30%; (ה) הגבלה קשה: 50%; (ו) הגבלה קשה מאוד: 70%
(ז) השגחה מתמדת: 100%

סעיף 33 - סכיזופרניה:
רמיסיה מלאה: 0%; הגבלה קלה: 10%; הגבלה בינונית: 25%
הגבלה ניכרת: 40%; ריבוי התקפים: 50%; הגבלה קשה: 70%; אשפוז מלא: 100%

====== פרק רביעי: שלד ומפרקים ======

סעיף 37 - עמוד השדרה:
הגבלה צווארית: קלה 10%, בינונית 20%, קשה 30%
הגבלה מתנית: קלה 10%, בינונית 20%, קשה 30%
קשיון צווארי נוח: 30%, לא נוח: 50%
קשיון מתני נוח: 30%, לא נוח: 40%-50%

סעיף 35 - ארתריטיס:
ללא השפעה: 0%; קלה: 10%; בינונית: 20%; ניכרת: 30%
קשיון פרקים קטנים: 50%; פרקים גדולים: 80%; כסא גלגלים: 100%

====== סעיף 40-41 - גפיים עליונים ======

אבדן גף עליון בכתף: ימין 80%, שמאל 70%
אבדן האמה: ימין 65%, שמאל 55%
אבדן היד בשורש: ימין 60%, שמאל 50%
קשיון כתף נוח: ימין 30%, שמאל 25%; לא נוח: ימין 50%, שמאל 40%
קשיון מרפק נוח: ימין 30%, שמאל 20%; לא נוח: ימין 40%, שמאל 30%
קשיון שורש יד נוח: ימין 20%, שמאל 15%; לא נוח: ימין 40%, שמאל 30%

====== סעיף 43 - קטיעת אצבעות ======

אגודל עם מסרק: ימין 25%, שמאל 20%
אגודל שני גלילים: ימין 20%, שמאל 15%
אגודל גליל סופי: ימין 15%, שמאל 10%
אצבע 2 עם מסרק: ימין 15%, שמאל 12%; גליל סופי: 5%
אצבע 3 עם מסרק: ימין 15%, שמאל 12%; גליל סופי: 5%
אצבע 4 או 5 עם מסרק: 10%; גליל סופי: 5%

====== סעיף 47-48 - גפיים תחתונים ======

קטיעת ירך: 80%; קטיעת שוק: 50%-55%
קשיון ירך נוח: 40%, לא נוח: 50%
קשיון ברך נוח: 30%, לא נוח: 50%

====== פרק ה': ראייה ======

סעיף 52: עיוורון מלא שתי עיניים: 100%
ראיה 6/60 בעין אחת, 6/6 בשניה: 30%; ראיה 1/60 בשתי עיניים: 90%

====== פרק ו': שמיעה ======

סעיף 72: ירידה 15-20 דציבל: 0-10%; ירידה 30 דציבל: 15-20%
ירידה 50 דציבל: 25-30%; ירידה 70 דציבל: 35-45%; מעל 80 דציבל: 50-60%

====== פרק ז': לארינקס ======

סעיף 71: צרידות קלה: 5-10%; קשה: 20%; אפוניה: 30%; טרכאוסטומיה: 50%

====== פרק ח': כליות ======

סעיף 22: כריתת כליה אחת: 30%; עם הפרעות בשנייה: 60%
תסמונת נפרוטית: 50%-80%

====== פרק ט': עור ======

סעיף 75: צלקות מכאיבות: 10%; נרחבות: 20%
פנים - מכערות: 10%; ניכרות: 20%-30%; מעוררות דחייה: 50%

====== פרק י': מוגבלות שכלית ======

סעיף 91: גבולית IQ 70-79: 20%; קלה IQ 55-69: 40%
בינונית IQ 40-54: 65%; קשה IQ 30-39: 75%; חמורה: 100%

====== פרק יא: פיברומיאלגיה ======

סעיף 92: ברמיסה: 0%; קלה: 10%; בינונית: 20%; ניכרת: 30%; קשה: 40%
`;

const sleep = (ms) => new Promise(resolve => setTimeout(resolve, ms));

async function callGemini(apiKey, prompt, attempt = 0) {
  const response = await fetch(
    `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`,
    {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        contents: [{ parts: [{ text: prompt }] }],
        generationConfig: { temperature: 0.2, maxOutputTokens: 2048 }
      })
    }
  );

  // Rate limit — wait and retry (up to 3 times)
  if (response.status === 429 && attempt < 3) {
    const retryAfter = parseInt(response.headers.get('Retry-After') || '60', 10);
    const waitMs = Math.min(retryAfter * 1000, 70000);
    await sleep(waitMs);
    return callGemini(apiKey, prompt, attempt + 1);
  }

  if (!response.ok) {
    const err = await response.json();
    throw new Error(err.error?.message || `שגיאה מ-Gemini API: ${response.status}`);
  }

  return response.json();
}

exports.handler = async (event) => {
  const headers = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'Content-Type',
    'Content-Type': 'application/json'
  };

  if (event.httpMethod === 'OPTIONS') {
    return { statusCode: 200, headers, body: '' };
  }

  if (event.httpMethod !== 'POST') {
    return { statusCode: 405, headers, body: JSON.stringify({ error: 'Method not allowed' }) };
  }

  const apiKey = process.env.GEMINI_API_KEY;
  if (!apiKey) {
    return { statusCode: 500, headers, body: JSON.stringify({ error: 'מפתח API לא מוגדר בשרת' }) };
  }

  let description;
  try {
    const body = JSON.parse(event.body);
    description = body.description;
  } catch {
    return { statusCode: 400, headers, body: JSON.stringify({ error: 'בקשה לא תקינה' }) };
  }

  if (!description) {
    return { statusCode: 400, headers, body: JSON.stringify({ error: 'חסר תיאור רפואי' }) };
  }

  const prompt = `אתה מומחה בתקנות הביטוח הלאומי הישראלי וספר המבחנים לקביעת נכות.

להלן תוכן ספר המבחנים:
${BOOK_CONTENT}

המצב הרפואי שתואר:
"${description}"

המשימה שלך:
1. מצא את כל הסעיפים הרלוונטיים בספר המבחנים
2. לכל סעיף — ציין את מספר הסעיף, שמו, ואחוזי הנכות המתאימים למצב שתואר
3. הסבר מדוע בחרת בדרגה הספציפית
4. חשב את הנכות הכוללת לפי שיטת "הכושר המופחת" (לא סכום רגיל)

ענה אך ורק בפורמט JSON הבא, ללא טקסט נוסף:
{
  "findings": [
    {
      "section": "מספר הסעיף",
      "name": "שם הפגימה",
      "percentage": 25,
      "description": "הסבר קצר מדוע נקבע האחוז הזה",
      "note": "הערות נוספות אם רלוונטי"
    }
  ],
  "total": 35,
  "calculation": "הסבר איך חושב הסכום הכולל",
  "summary": "סיכום כולל של המצב לפי ספר המבחנים"
}`;

  try {
    const data = await callGemini(apiKey, prompt);
    const text = data.candidates?.[0]?.content?.parts?.[0]?.text || '';

    try {
      const cleaned = text.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim();
      const parsed = JSON.parse(cleaned);
      return { statusCode: 200, headers, body: JSON.stringify(parsed) };
    } catch {
      return { statusCode: 200, headers, body: JSON.stringify({ raw: text }) };
    }

  } catch (err) {
    return {
      statusCode: 500,
      headers,
      body: JSON.stringify({ error: err.message || 'שגיאה פנימית' })
    };
  }
};
